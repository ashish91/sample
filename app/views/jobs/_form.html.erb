<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
        <% job.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions row w-25 px-3">
    <div class='col'>
      <%= form.text_field :title, placeholder: 'Add title', class: 'form-control' %>
    </div>
  </div>

  <div class="actions row w-25 px-3">
      <div class='col'>
      <%= form.submit %>
    </div>
  </div>
<% end %>
